{
  "id": "loading-spinner-root",
  "type": "div",
  "props": {
    "role": "status",
    "aria-label": "Loading"
  },
  "bindings": {
    "className": {
      "source": "size",
      "transform": "const sizes = { sm: 'w-4 h-4 border-2', md: 'w-6 h-6 border-2', lg: 'w-8 h-8 border-3' }; return `inline-block ${sizes[data || 'md']} border-primary border-t-transparent rounded-full animate-spin ${props.className || ''}`"
    }
  }
}
