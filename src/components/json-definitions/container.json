{
  "id": "container-root",
  "type": "div",
  "bindings": {
    "className": {
      "source": "size",
      "transform": "const sizes = { sm: 'max-w-screen-sm', md: 'max-w-screen-md', lg: 'max-w-screen-lg', xl: 'max-w-screen-xl', full: 'max-w-full' }; const cn = (await import('@/lib/utils')).cn; return cn('mx-auto px-4 sm:px-6 lg:px-8', sizes[data || 'xl'], props.className)"
    }
  },
  "children": "{{children}}"
}
