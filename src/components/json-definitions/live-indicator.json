{
  "id": "live-indicator-container",
  "type": "div",
  "bindings": {
    "className": {
      "source": ["size", "className"],
      "transform": "const sizeClasses = { sm: 'text-xs gap-1.5', md: 'text-sm gap-2', lg: 'text-base gap-2.5' }; return ['inline-flex items-center font-medium', sizeClasses[data[0] || 'md'], data[1]].filter(Boolean).join(' ')"
    }
  },
  "children": [
    {
      "id": "dot-container",
      "type": "span",
      "bindings": {
        "className": {
          "source": null,
          "transform": "'relative flex'"
        }
      },
      "children": [
        {
          "id": "dot-pulse",
          "type": "span",
          "bindings": {
            "className": {
              "source": "size",
              "transform": "const dotSizes = { sm: 'w-2 h-2', md: 'w-2.5 h-2.5', lg: 'w-3 h-3' }; return ['absolute inline-flex rounded-full bg-red-500 opacity-75 animate-ping', dotSizes[data || 'md']].join(' ')"
            }
          }
        },
        {
          "id": "dot-main",
          "type": "span",
          "bindings": {
            "className": {
              "source": "size",
              "transform": "const dotSizes = { sm: 'w-2 h-2', md: 'w-2.5 h-2.5', lg: 'w-3 h-3' }; return ['relative inline-flex rounded-full bg-red-500', dotSizes[data || 'md']].join(' ')"
            }
          }
        }
      ]
    },
    {
      "id": "label",
      "type": "span",
      "bindings": {
        "_if": {
          "source": "showLabel",
          "transform": "data !== false"
        },
        "className": {
          "source": null,
          "transform": "'text-red-500 font-bold tracking-wider'"
        },
        "children": {
          "source": "label",
          "transform": "data || 'LIVE'"
        }
      }
    }
  ]
}
